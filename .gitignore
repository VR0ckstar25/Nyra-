# OS junk
.DS_Store
**/.DS_Store

# Node
node_modules/

# Android build outputs / machine-specific
sfis-android/.gradle/
sfis-android/build/
sfis-android/app/build/
sfis-android/local.properties
*.apk

# Android wrapper assets are synced from /replica by sync-and-build.sh (generated)
sfis-android/app/src/main/assets/

# Secrets — OAuth client ids / Firebase keys (app UI instructs creating .env)
.env
.env.*
sfis-app/.env

# Expo local state
sfis-app/.expo/

# Secrets — OAuth client ids / Firebase keys live in .env (review finding)
.env
.env.*
sfis-app/.env
sfis-app/.env.*

# Secrets — OAuth client IDs / Firebase keys live in .env (never commit)
.env
.env.*
!.env.example

# Native OCR module build artifacts
sfis-app/modules/anvara-ocr/android/.gradle/
sfis-app/modules/**/build/

# Firebase emulator artifacts
sfis-app/firestore-debug.log
sfis-app/firebase-debug.log
sfis-app/ui-debug.log
sfis-app/*-debug.log
