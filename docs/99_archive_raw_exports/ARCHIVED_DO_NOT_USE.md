# Archived Raw Exports

The stale raw conversation/export artifacts that contained superseded build guidance
were removed from the working folder.

Do not restore raw exports as implementation sources. If a future export is needed for
history, reconcile it into the active docs first and keep this folder clearly marked as
non-authoritative.

Use `../README.md` for the active document order.
